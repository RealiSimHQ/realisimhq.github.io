{
  "name": "realsimhq-local-sim-setup",
  "version": "1.0.0",
  "private": true,
  "description": "RealiSimHQ local simulator setup booking landing page",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:ci": "npm run test"
  }
}
